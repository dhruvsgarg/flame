{
  "taskid": "refl_ablation_baseline_experiment",
  "backend": "mqtt",
  "brokers": [
    {
      "host": "localhost",
      "sort": "mqtt"
    }
  ],
  "groupAssociation": {
    "param-channel": "default"
  },
  "channels": [
    {
      "description": "Model update is sent from trainer to aggregator and vice-versa",
      "groupBy": {
        "type": "tag",
        "value": ["default"]
      },
      "name": "param-channel",
      "pair": ["trainer", "aggregator"],
      "funcTags": {
        "aggregator": ["distribute", "aggregate"],
        "trainer": ["fetch", "upload"]
      }
    }
  ],
  "selector": {
    "sort": "refl_oort",
    "kwargs": {
      "aggr_num": 10,
      "avail_priority": 0,
      "blacklist_rounds": -1,
      "pacer_step": -1,
      "trainer_registry_file": "metadata/trainer_registry.yaml",
      "exploration_factor": 0.9,
      "exploration_decay": 0.98,
      "exploration_min": 0.2
    }
  },
  "optimizer": {
    "sort": "fedavg"
  },
  "registry": {
    "sort": "mlflow",
    "uri": "tmp/mlflow"
  },
  "job": {
    "id": "ablation-baseline",
    "name": "Baseline FedAvg (no REFL features)"
  },
  "hyperparameters": {
    "rounds": 100,
    "epochs": 1,
    "batchSize": 32,
    "learningRate": 0.01,
    "aggGoal": 10,
    "trackTrainerAvail": {
      "enabled": "True",
      "type": "ORACULAR",
      "trace": "avl_events_syn_20"
    }
  },
  "dataset": "https://storage.googleapis.com/tensorflow/tf-keras-datasets/mnist.npz",
  "dependencies": ["numpy >= 1.2.0"],
  "baseModel": {
    "name": "",
    "version": 2
  },
  "realm": "cifar10-refl-baseline",
  "role": "aggregator"
}
